# NIE Tamil Math Tutor — Enterprise Multi-Agent System
# requirements.txt

# Core LLM
google-genai>=1.0.0           # Gemini API (primary LLM backend)
python-dotenv>=1.0.0           # .env loading

# Vector database
chromadb>=0.5.0                # local persistent vector DB (PoC)
# qdrant-client>=1.9.0         # uncomment for production migration

# Embeddings (multilingual, supports Tamil + Sinhala)
sentence-transformers>=3.0.0   # BAAI/bge-m3 and multilingual-e5-large

# PDF extraction
pymupdf>=1.24.0                # PyMuPDF — fitz — primary PDF reader
pytesseract>=0.3.10            # OCR for image-based PDFs
Pillow>=10.0.0                 # image processing for OCR

# Ollama fallback (optional local LLM)
ollama>=0.2.0
httpx>=0.27.0

# Utilities
numpy>=1.26.0
tqdm>=4.66.0                   # progress bars during embedding

---
# SYSTEM DEPENDENCIES (install separately)
# Ubuntu/Debian:
#   sudo apt-get install tesseract-ocr tesseract-ocr-tam tesseract-ocr-sin
# macOS:
#   brew install tesseract
#   pip install tesseract-ocr-tam  # Tamil language pack
